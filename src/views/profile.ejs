<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Profile Page</title>
</head>
<%- include('common/header') %>
<body>
    <h1>Hello <%= user.name %> </h1>
    <h2>Email <%= user.email %> </h2>
    <ul>
        <% user.skilss.forEach((items) => { %>
            <li><%= items %></li>
        <% }) %>
    </ul>
</body>
</html>